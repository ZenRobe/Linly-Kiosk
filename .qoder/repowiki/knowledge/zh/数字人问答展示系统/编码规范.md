- 采用配置驱动开发模式，所有业务数据（问答、视频路径）与 UI 文本均外置到 `kiosk_config.py` 中。
- 在 Gradio Python 逻辑中通过 `.then(js="...")` 链式调用触发前端 JavaScript 函数，实现前后端状态同步。
- 使用 Python f-string 或 `.format()` 将后端配置变量动态注入到前端 CSS 和 JavaScript 字符串模板中。